/**
 * Created by lahim on 12/03/15.
 */

angular.module("angapp.controllers")
    .controller(
    "LogoutCtrl",
    [
        "$scope",
        "$rootScope",
        "$http",
        "$cookies",
        function ($scope, $rootScope, $http, $cookies) {
        }
    ]
);